package com.bnta.week_two_thurs;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.io.PrintWriter;

/*
public class WorkingWithFiles {
    public static void main(String[] args)  throws IOException {}//  IOException extends Exceptions so it is a child (form) for checked
            //used when working with files etc


 */

    /*
        // create file
        try
        {
        File file = new File("src/hello.txt");
        if (!file.exists()) {  //this is saying if the file does not exist create a new one
            file.createNewFile();
        }


            { FileWriter fileWriter = new FileWriter(file);
        PrintWriter printWriter = new PrintWriter(fileWriter);
        printWriter.println("hello");
        printWriter.println("Bye");

        // once you are done writing to the file
        printWriter.flush(); //writes the contents
        printWriter.close(); // closes the string

    } catch (IOException e) {e.printStacckTrace();
}


     */